#include <iostream>

struct Edge {
  int start, finish;
  int flow, capacity;

};

int main() {
  int n, m, start, finish;
  std::cin >> n >> m >> start >> finish;
  for (int i = 0; i < m; ++i) {
    int from, to;
    std::cin >> from >> to;
    
  }
}