#include <iostream>

int main() {
  int n, m;
  std::cin >> n >> m;
  for (int i = 0; i < n; ++i) {
    int current_number;
    std::cin >> current_number;

  }
  for (int i = 0; i < m; ++i) {
    int start, end;
    std::cin >> start >> end;
  }
}